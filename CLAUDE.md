@AGENTS.md

## Agent skills

### Issue tracker

Issues live in **Linear**, created and read via the Linear MCP server. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout (`CONTEXT-MAP.md` at root pointing to per-context `CONTEXT.md` files). See `docs/agents/domain.md`.
